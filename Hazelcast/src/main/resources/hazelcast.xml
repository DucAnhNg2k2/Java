<?xml version="1.0" encoding="UTF-8"?>
<hazelcast xmlns="http://www.hazelcast.com/schema/config"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://www.hazelcast.com/schema/config
           http://www.hazelcast.com/schema/config/hazelcast-config-5.7.xsd">

    <cluster-name>dev-cluster</cluster-name>

    <properties>
        <!-- Không gửi metrics ẩn danh về Hazelcast -->
        <property name="hazelcast.phone.home.enabled">false</property>
    </properties>

    <network>
        <port auto-increment="true" port-count="20">5701</port>
        <join>
            <!-- Tắt multicast để không vô tình join cluster của máy khác trong LAN -->
            <multicast enabled="false"/>
            <tcp-ip enabled="true">
                <member>127.0.0.1</member>
            </tcp-ip>
        </join>
    </network>

    <map name="users">
        <backup-count>1</backup-count>
        <time-to-live-seconds>0</time-to-live-seconds>
    </map>
</hazelcast>
